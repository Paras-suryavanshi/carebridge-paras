# --- Core Django ---
Django>=5.0
djangorestframework
django-cors-headers
Pillow                  # For image handling (avatars)

# --- AI & Cloud Services ---
google-generativeai      # For Gemini Chat
azure-cognitiveservices-speech  # For Voice-to-Text
azure-ai-textanalytics   # For Sentiment/Mood (if you used this)
requests                 # Helper for HTTP calls

# --- Configuration & Security ---
python-dotenv            # To read .env files
django-environ           # (Optional but good) Helper for env vars

# --- Database (Render Requirement) ---
dj-database-url          # Connects Django to Render's DB URL
psycopg2-binary          # PostgreSQL Adapter

# --- Production Server (Render Requirement) ---
gunicorn                 # The web server that runs Django in production